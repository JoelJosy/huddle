import { SignOutButton } from "@/components/auth/SignOutButton";
import React from "react";

const page = () => {
  return (
    <>
      <h1>Dashboard</h1>;
      <SignOutButton />
    </>
  );
};

export default page;
